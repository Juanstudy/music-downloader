# OpenSpec Configuration — music-dowloader
# Auto-generated by gentle-ai sdd-init
generated_at: 2026-07-13

context: |
  TUI music downloader using yt-dlp as engine. Go + Bubble Tea.
  MVP: YouTube URLs only, sequential downloads, MP3 output with metadata.
  Architecture: provider interfaces (Searcher/Downloader), queue in memory.

stack:
  language: Go (1.26)
  framework: Bubble Tea (Charm)
  build: go build
  arch: clean/hexagonal with provider interfaces

testing:
  runner:
    command: go test -short ./...
    framework: Go test (standard library)
    available: true
    reason: go.mod initialized (go 1.26.3); unit tests run with -short
  layers:
    unit: { available: true, tool: go test }
    integration:
      {
        available: true,
        tool: go test (no -short; requires yt-dlp + ffmpeg + network),
      }
    e2e: { available: false, tool: null }
  coverage:
    available: true
    command: go test -cover ./...
  quality:
    linter: { available: false, command: null }
    formatter: { available: true, command: gofmt }
    type_checker: { available: true, command: go build ./... }

strict_tdd: true
strict_tdd_reason: go.mod initialized and go test works; session harness enforces Strict TDD.

phases:
  proposal:
    required: true
    artifacts: proposal
  spec:
    required: true
    artifacts: spec
  design:
    required: true
    artifacts: design
  tasks:
    required: true
    artifacts: tasks
  apply:
    required: true
    artifacts: apply-progress
  verify:
    required: true
    artifacts: verify-report
  archive:
    required: true
    artifacts: archive-report
